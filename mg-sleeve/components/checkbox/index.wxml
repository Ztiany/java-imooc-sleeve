<view
        bind:tap="onCheck"
        class="container {{checked?'bg-checked':'bg-unchecked'}}">

    <l-icon
            wx:if="{{checked}}"
            name="check"
            size="28"
            l-class="sleeve"
    ></l-icon>

</view>
